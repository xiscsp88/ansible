---
- hosts: 192.168.0.8
  gather_facts: yes

- name : Users 
  user: 
    name:{{ lookup('env','USERNAME') }} 
    createhome:yes 
    home:/home/{{ lookup('env','USERNAME') }} 
    generate_ssh_key:yes 
    shell:/bin/bash 
    password:{{ lookup('env','PASS') }}
